{
  "name": "{{ name }}",
  "version": "0.0.1",
  "description": "{{ description }}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "{{ author }}",
  "license": "ISC",
  "bin": {
    "chaicn": "index.js"
  },
  "dependencies": {
    "commander": "^6.2.0",
    "download-git-repo": "^3.0.2"
  }
}
